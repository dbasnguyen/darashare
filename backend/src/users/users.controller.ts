// import { Controller, Post, Body } from '@nestjs/common';
// import { UsersService } from './users.service';
// 
// @Controller('users')
// export class UsersController {
//  constructor(private readonly usersService: UsersService) {}

  // ============================================================
  // 📝 CRÉATION D’UN UTILISATEUR (non utilisée dans ton MVP)
  // ------------------------------------------------------------
  // ⚠️ Dans ton projet, l’inscription se fait via /auth/register.
  // Ce contrôleur pourrait être supprimé ou réservé à l’admin.
  // ============================================================
  // @Post()
  // async create(@Body() body: { email: string; password: string }) {
  //  return this.usersService.create(body.email, body.password);
  // }
// }
